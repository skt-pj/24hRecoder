plugins {
    id 'com.android.application'
    id 'org.jetbrains.kotlin.android'
    id 'org.jetbrains.kotlin.plugin.compose'
}

android {
    namespace 'com.sktpj.recorder24h'
    compileSdk 36
    ndkVersion '27.0.12077973'

    defaultConfig {
        applicationId 'com.sktpj.recorder24h'
        minSdk 29
        targetSdk 36
        versionCode 35
        versionName '0.6.14'
        // Normal local transcription model is user-selectable in Settings.
        // Recording input verifies actual Bluetooth routing and falls back to the device mic if routing fails.
        // Record-list scroll/search/filter context survives a detail-screen round trip.
        // PCM is gated while recording so definite silence skips decode/VAD/Whisper and candidate ranges keep original timestamps.
        // Automatic and visible transcription now share one recorded-start FIFO dispatcher.
        // WorkManager wakes the FIFO but no longer runs one competing Whisper worker per segment.

        ndk {
            abiFilters 'arm64-v8a'
        }

        externalNativeBuild {
            cmake {
                cppFlags '-std=c++17'
            }
        }
    }

    buildTypes {
        debug {
            applicationIdSuffix '.debug'
            versionNameSuffix '-debug'
            // CI applies a fixed development-only signing certificate after assembleDebug.
            // Do not use that certificate for production releases.
            signingConfig null
        }
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    externalNativeBuild {
        cmake {
            path file('src/main/cpp/CMakeLists.txt')
            version '3.22.1'
        }
    }

    buildFeatures {
        compose true
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}

kotlin {
    jvmToolchain(17)
}

dependencies {
    implementation 'androidx.work:work-runtime:2.11.2'
    implementation 'com.github.k2-fsa:sherpa-onnx:1.13.4'

    def composeBom = platform('androidx.compose:compose-bom:2026.06.00')
    implementation composeBom

    implementation 'androidx.activity:activity-compose:1.13.0'
    implementation 'androidx.compose.ui:ui'
    implementation 'androidx.compose.ui:ui-tooling-preview'
    implementation 'androidx.compose.material3:material3'
    implementation 'androidx.compose.material:material-icons-extended'
}
